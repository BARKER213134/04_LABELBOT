<analysis>**original_problem_statement:**
The user wants to create a Telegram bot for generating shipping labels using the ShipEngine API.

PRODUCT REQUIREMENTS:
- A scalable system with a beautiful, animated interface.
- A web dashboard for management.
- A database (MongoDB) to store user and shipment history.
- A complete user management system where each user has a balance manageable from the admin panel.
- A professional, multi-step wizard conversation flow for data entry in the bot.
- Support for both test and production ShipEngine environments.
- The wizard must include a summary and an option to edit data before final confirmation.
- Prices fetched from ShipEngine must have a 0 markup.
- Implement cryptocurrency payments via OxaPay.
- An AI should generate a unique, uplifting thank-you message in Russian after label creation.
- The bot should download the label PDF from ShipEngine and send it as a file.
- An admin panel feature for mass messaging, user management, and order statistics (highlighting orders with < 0 profit).
- A maintenance mode in the admin panel.
- Support English and Russian languages (i18n).

**User's preferred language**: Russian. The next agent MUST respond in Russian.

**what currently exists?**
A full-stack application (FastAPI backend, React admin panel, MongoDB) for a Telegram shipping bot. In this session, critical stability, financial, and deployment issues were resolved. The bot's startup sequence was optimized to prevent Kubernetes health check timeouts. A major financial bug where the bot undercharged users for large packages (missing insurance/dimensional weight fees) was fixed. A health monitor was added, and the ShipEngine API fallback mechanism was corrected using the user's actual Carrier IDs.

**Last working item**:
- **Last item agent was working**: Implementing a hybrid webhook processing model in . 
*Reasoning:* The production server kept returning HTTP 520 and crashing after a few hours. The agent diagnosed that synchronous processing of long ShipEngine API calls (30-40 seconds) in the webhook route tied up the single Uvicorn worker. This prevented the server from responding to Kubernetes health checks, causing K8s to kill and restart the pod. The agent just applied a fix to process fast updates synchronously and heavy tasks in a background , but the session ended before verification.
- **Status**: TESTING PENDING
- **Agent Testing Done**: N
- **Which testing method agent to use?** backend testing agent / manual testing by curl to ensure the  endpoint responds immediately while processing heavy payloads in the background.
- **User Testing Done**: N

**All Pending/In progress Issue list**:
- Issue 1: Production Pod Restarts / HTTP 520 Errors (P0)
  - Attempted fixes: Made bot preloading asynchronous, moved route imports out of lifespan. Finally, changed webhook processing to a hybrid async/sync model.
  - Next debug checklist: Check  to ensure  for heavy updates doesn't lose context or break 's . Verify health checks pass during label generation.
  - Why fix this issue and what will be achieved with the fix? Prevents the Kubernetes pod from being killed during long ShipEngine API calls, stabilizing production.
  - Status: TESTING PENDING
  - Is recurring issue? Y
  - Should Test frontend/backend/both after fix? backend
  - Blocked on other issue: None

- Issue 2: Incorrect weight/dimensions when loading from a template (P2)
  - Attempted fixes: None yet.
  - Next debug checklist: Review  ->  and  -> .
  - Why fix this issue and what will be achieved with the fix? Ensures users see accurate data when reusing shipping templates.
  - Status: NOT STARTED
  - Is recurring issue? N
  - Should Test frontend/backend/both after fix? backend

- Issue 3: SITKAGEAR text appears on UPS labels (P3)
  - Attempted fixes: None. Believed to be a ShipEngine dashboard account setting.
  - Next debug checklist: Ask user to verify if this still happens on production labels.
  - Status: USER VERIFICATION PENDING
  - Is recurring issue? Y
  - Should Test frontend/backend/both after fix? NA

**In progress Task List**:
None.

**Upcoming and Future Tasks**:
- **Upcoming Tasks:**
  - FedEx Sandbox Handling (P2): Hide FedEx as a carrier option when the bot is in sandbox mode.
- **Future Tasks:**
  - Refactor  (P1): File is ~3000 lines. Break it down into smaller modules per conversation step.
  - Refactor  (P2): Break down massive React component into smaller, reusable pieces.

**Completed work in this session**:
- Fixed infinite recursion  in  across multiple files.
- Fixed backend deployment  by moving  to a background task so Kubernetes health checks pass instantly.
- Fixed critical financial bug: included  and  in rate estimates, and added a safeguard in  to charge actual label cost if it exceeds the estimate.
- Added WhiteLabelPlatform.cc promotional button to bot's welcome and main menus.
- Created  to check DB, Bot, API, and Webhooks hourly and alert the admin via Telegram.
- Fixed MongoDB DNS  errors on startup by adding exponential backoff retry logic in .
- Fixed ShipEngine fallback mechanism: Replaced dummy Carrier IDs with user's actual IDs (, , ).
- Optimised MongoDB queries in  (added  limits).
- Filtered out  carrier to prevent constant API warnings.

**Earlier issues found/mentioned but not fixed**:
None outside of the pending list.

**Known issue recurrence from previous fork**:
- Issue recurrence in previous fork: Production Deployment Failures / Server Crashes.
- Recurrence count: 3+
- Status: TESTING PENDING (Believed to be solved by the final hybrid webhook fix, but K8s health check timeouts have been a persistent theme).

**Code Architecture**:


**Key Technical Concepts**:
- **Kubernetes Health Checks vs. Single Worker:** FastAPI running on a single Uvicorn worker will fail K8s liveness probes if a single request (like a webhook triggering ShipEngine) takes 30+ seconds.
- **ShipEngine Cost Discrepancies:** Dimensional weight and insurance can make the final  drastically higher than the base . 

**key DB schema**:
- : Manages python-telegram-bot state.
- : Stores label info.
- : Tracks OxaPay crypto top-ups.

**changes in tech stack**:
None.

**All files of reference**:
- : App initialization and background tasks.
- : Webhook endpoints (crucial for current pending issue).
- : Carrier logic and pricing math.
- : Core bot logic.
- : System status reporter.

**Areas that need refactoring**:
-  (High priority)

**key api endpoints**:
- : Receives updates from Telegram.
-  & : Used by Kubernetes for pod liveness.

**Critical Info for New Agent**:
- **DO NOT** make the entire webhook processing fully synchronous. The last action taken was implementing a hybrid webhook in  where quick updates are awaited directly, but heavy operations are pushed to . You MUST test if this successfully prevents the K8s pod from timing out and crashing (HTTP 520) while still allowing  to function properly.
- The system heavily relies on 's . Ensure background tasks don't cause race conditions with persistence.

**documents and test reports created in this job**:
-  (Updated)
- 

**Last 10 User Messages and any pending HUMAN messages**:
1. User: Notes that the bot is spamming the health check every 5 minutes. (Agent fixed).
2. User: Uploads image of a document verification UI. (Agent ignored, wrong project).
3. User: Confirms it's the wrong project.
4. User: бот перестал работать (Bot stopped working again).
5. User: Provides deployment logs showing the bot worked for ~3 hours then crashed.
6. User: сейчас 520 (Reporting production is down).
7. User: Provides deployment logs showing successful startup, but followed by a crash.
(Agent diagnosed the K8s health check timeout issue and pushed a fix just before the session ended).

**Project Health Check**:
- Broken (Production server is highly unstable and restarts every few hours due to webhook processing blocking health checks. A potential fix is in the code but unverified).

**3rd Party Integrations**:
- ShipEngine API — requires User API Key (Configured in DB/Settings)
- Telegram Bot API — requires User API Key
- OxaPay (Payments) — requires User API Key
- OpenAI GPT-4o-mini (Thank you messages) — uses Emergent LLM Key

**Testing status**:
- Testing agent used after significant changes: YES (Early in session, but NOT for the final webhook changes).
- Troubleshoot agent used after agent stuck in loop: NO
- Test files created: None
- Known regressions: Webhook background processing might interfere with PTB's ConversationHandler state. Needs verification.

**Credentials to test flow**:
- Admin Panel Login:  / 
- Admin Telegram ID: 

**What agent forgot to execute**:
The agent did not test the final  modification made to  (the hybrid webhook logic) before the session ended. Testing this is priority #1.</analysis>
